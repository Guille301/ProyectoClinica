@model IEnumerable<DTOs.Evolucion.EvolucionListaDto>

@{
    ViewData["Title"] = "GetEvoluciones";
}

<h1>GetEvoluciones</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdHistoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionEvolucion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdHistoria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionEvolucion)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
